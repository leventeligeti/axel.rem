[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "axel-rem"
version = "0.1.0"
description = "Multi-agent semantic memory engine — prompt-chill pipeline + nightly dream consolidation"
requires-python = ">=3.11"
dependencies = [
    "sentence-transformers>=2.7",
    "redis>=5.0",
    "psycopg2-binary>=2.9",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["axel_rem*"]
